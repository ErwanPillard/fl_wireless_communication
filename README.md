#  Distributed and Semi-distributed Machine Learning schemes over wireless environments

Final year project

## Installation
```bash
python -m venv env
source env/bin/activate
pip install -r requirements.txt
```
## Run Baseline
```bash
source env/bin/activate
chmod +x experiments/run_baseline.sh
./experiments/run_baseline.sh
```